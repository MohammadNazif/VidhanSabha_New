@{
    ViewBag.Title = "Panna List by Sector";
    Layout = "~/Views/Shared/_LayoutSector.cshtml";

    var panna = ViewData["PannaList"] as List<VishanSabha.Models.PannaPramukh>;
    var casteList = ViewData["CasteList"] as List<VishanSabha.Models.SubCaste>;
}

<style>
    body {
        background-color: #fdfaf6;
        font-family: 'Poppins', sans-serif;
    }

    .table-head {
        background-color: #FF8C42;
        color: white !important;
        /*        display:none;*/
    }

    .myhead {
        font-size: 12px;
        color: white;
        padding: 15px 10px;
    }

    .table-head th, #pannaTable td {
        text-align: left !important;
        vertical-align: middle;
        padding: 2px 6px;
        font-size: 11px;
    }

    #pannaTable img {
        width: 50px !important;
        height: auto;
    }

    /*.table-responsive {
        overflow-x: auto;
        white-space: nowrap;
        max-height: 80vh !important;
    }*/

    #pannaTable td,
    #pannaTable th {
        white-space: nowrap;
        padding: 3px 6px !important; /* kam padding → row height kam */
        font-size: 11px !important; /* chhota text */
        line-height: 1.2 !important; /* compact vertical spacing */
        height: 26px !important;
    }

    #pannaTable th {
        color: white !important;
    }

    .btn-outline-orange {
        border-color: orange;
        color: orange;
        font-size: 12px;
        padding: 4px 10px;
    }

        .btn-outline-orange:hover {
            background-color: #FF8C42;
            color: white;
        }

    .btn-xs {
        padding: 1px 5px;
        font-size: 8px;
        line-height: 1.2;
        border-radius: 3px;
    }

    .edit-icon, .delete-icon {
        font-size: 14px;
    }

    .edit-icon {
        color: green;
    }

    .edit-btn:hover .edit-icon {
        color: white;
    }

    .delete-icon {
        color: red;
    }

    .delete-btn:hover .delete-icon {
        color: white;
    }

    .btn-sm {
        padding: 2px 6px;
        font-size: 12px;
    }

    .dataTables_length select {
        font-size: 12px !important;
        padding: 2px 5px !important;
        width: 54px !important;
        height: 28px !important;
    }

    .dataTables_filter input {
        font-size: 12px !important;
        padding: 3px 6px !important;
        width: 150px !important;
        height: 28px !important;
        border-radius: 4px;
    }

    div.dataTables_filter input[type="search"] {
        border: 1px solid #FF8C42 !important;
    }

    #myTable_paginate .paginate_button {
        font-size: 12px; /* font size chhota ya bada karne ke liye */
        margin-top: 10px;
        padding: 5px 10px; /* button ke andar ki padding */
        min-width: 40px; /* button ki minimum width */
        height: 20px; /* button ki height */
        border-radius: 5px; /* corners round karne ke liye */
        margin: 0 2px; /* buttons ke beech thodi gap */
        cursor: pointer;
        background-color: #FF8C42;
        color: black;
        border: none;
    }

    #exportIcons a {
        text-decoration: none;
    }


    .table-wrapper {
        margin-top: 10px; /* 👈 filter section ke neeche gap */
        background: #ffffff;
        border-radius: 8px;
        padding: 10px 15px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    }




    .table-controls {
        position: sticky;
        top: 0;
        z-index: 15;
        background: #fff;
        /*        padding: 8px 0;*/
        border-bottom: 1px solid #ddd;
    }

    .table-scroll {
        max-height: 400px; /* adjust height */
        overflow-y: auto;
        overflow-x: auto;
        position: relative;
        overflow: auto;
        z-index: 1;
        /*        padding-top:80px;*/
    }

        .table-scroll table {
            width: 100%;
            border-collapse: collapse;
        }

        /* Sticky header */
        .table-scroll thead th {
            /*                    position: sticky !important;*/
            top: 0;
            background-color: #FF8C42;
            color: white;
            z-index: 10;
            box-shadow: 0 2px 2px rgba(0, 0, 0, 0.1);
        }

        .table-scroll::-webkit-scrollbar {
            height: 8px;
            width: 8px;
        }

        .table-scroll::-webkit-scrollbar-thumb {
            background-color: #a4a4a4;
            border-radius: 4px;
        }

    .table-responsive {
        max-height: 400px; /* scroll area height */
        overflow-y: auto;
    }

    .select2-container {
        z-index: 999 !important;
    }

    .container-fluid {
        background: #ffffff;
        border-radius: 10px;
        padding: 20px;
        box-shadow: 0 3px 10px rgba(0, 0, 0, 0.05);
        margin-top: 60px !important; /* 👈 Header ke niche thoda extra space */
        position: relative;
        z-index: 1;
    }

    .dataTables_length label {
        font-size: 13px;
        font-weight: 500;
        color: #333;
        margin-right: 6px; /* 👈 "Show" aur dropdown ke beech gap */
        letter-spacing: 0.5px; /* 👈 thoda wide look */
    }

    div.dataTables_filter input[type="search"] {
        border: 1px solid #FF8C42 !important; /* Default border */
        outline: none !important; /* Default blue outline hatayega */
        box-shadow: none !important; /* Shadow remove karega */
    }

        div.dataTables_filter input[type="search"]:focus {
            border: 1px solid #FF8C42 !important; /* 👈 Focus hone par bhi same border */
            outline: none !important;
            box-shadow: none !important;
        }

    .divSize {
        width: 150px !important;
    }

    .dataTables_length select {
        font-size: 12px !important;
        padding: 2px 5px !important;
        width: 54px !important;
        height: 28px !important;
    }

    .dataTables_filter input {
        font-size: 12px !important;
        padding: 3px 6px !important;
        width: 150px !important;
        height: 28px !important;
        border-radius: 4px;
    }

    .table-wrapper {
        height: 500px !important;
        overflow: hidden;
        background: #ffffff;
        border-radius: 8px;
        padding: 10px 15px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        display: flex;
        flex-direction: column;
    }

    #entriesAndExport {
        position: absolute;
        top: 20px !important;
        left: 230px !important;
        background: white;
        z-index: 10;
    }
    /*    .dt-layout-row:first-child {
        position: fixed !important;
        top: 0px;
        left: 0;
        width: 100%;
        z-index: 100;
        background: white;
    }*/

    .dt-scroll-body .myhead {
        display: none !important;
    }

    .dt-scroll-body tbody td {
        min-width: 110px !important;
    }

    #pannaTable th:nth-child(1),
    #pannaTable td:nth-child(1) {
        width: 40px !important;
        max-width: 190px !important;
        min-width: 100px !important;
        text-align: center;
        white-space: nowrap;
    }
</style>

<div class="container-fluid mt-5 m-3">
    <h4 style="color: #FF8C42;" class="mb-3">Panna Pramukh List</h4>

    <div class="row g-1">
        @*<div class="col-sm-2">
                <select class="form-select form-select-sm shadow-sm !rounded-pill border-primary" multiple id="boothFilter">

                    @if (BoothList != null && BoothList.Any())
                    {
                        foreach (var data in BoothList)
                        {
                            <option value="@data.Booth_Id">@data.BoothNumber</option>
                        }
                    }
                </select>
            </div>*@
        <div class="col-sm-2 divSize">
            <select class="form-select form-select-sm shadow-sm rounded-pill border-primary" multiple id="casteFilter">

                @if (casteList != null && casteList.Any())
                {
                    foreach (var data in casteList)
                    {
                        <option value="@data.SubCaste_Id">@data.SubCasteName</option>
                    }
                }
            </select>
        </div>
        <div class="col-sm-2 divSize">
            <button type="button" class="btn btn-outline-secondary w-100 sqaure_filter shadow-sm " id="filterButton" title="Filters">
                <i class="bi bi-arrow-clockwise"></i> Filter
            </button>
        </div>
        <div class="col-sm-2 divSize">
            <button type="button" class="btn btn-outline-secondary w-100 sqaure_filter shadow-sm" id="resetFilters" title="Reset Filters">
                <i class="bi bi-arrow-clockwise"></i> Reset Filter
            </button>
        </div>
    </div>

    <div class="table-wrapper  shadow rounded p-2">
        <div class="table-controls d-flex justify-content-between align-items-center mb-2 flex-wrap ">
            <div id="entriesAndExport" class="d-flex align-items-center gap-2">
                <div id="exportIcons" class="d-flex align-items-center gap-1">
                    <!-- Excel Export -->
                    <a href="#" id="BoothDownloadExcel" title="Download Excel">
                        <i class="bi bi-file-earmark-excel-fill"
                           style="font-size: 1.3rem; color: #1D6F42; cursor:pointer;"></i>
                    </a>

                    <!-- PDF Export -->
                    <a href="#" id="BoothDownloadPdf" title="Download PDF">
                        <i class="bi bi-file-earmark-pdf-fill"
                           style="font-size: 1.3rem; color: #D32F2F; cursor:pointer;"></i>
                    </a>
                    @*<a href="@Url.Action("sectorPannaPramukhExportReport", "Export", new { format = "excel" })"
                           <i class="bi bi-file-earmark-excel-fill"  style="font-size: 1.3rem; color: #1D6F42; cursor:pointer;"></i>
                        </a>

                        <a href="@Url.Action("sectorPannaPramukhExportReport", "Export", new { format = "pdf" })"
                        <i class="bi bi-file-earmark-pdf-fill"  style="font-size: 1.3rem; color: #D32F2F; cursor: pointer;"></i>
                        </a>*@
                </div>

            </div>
            <div id="searchBox"></div>
        </div>
        <div class="  " style="white-space:nowrap!important;">

            <table id="pannaTable" class="table table-responsive-lg table-hover table-bordered align-middle">


                <thead class="table-head" style="color:white!important;">
                    <tr class="text-light myhead">
                        <th style="text-align:center!important;color:white!important;">S.N.</th>
                        <th style="color: white !important;">Mandal</th>
                        <th style="color: white !important;">Sector Name</th>
                        <th style="text-align:center!important;color:white!important;">Booth No</th>
                        @*<th>Booth Name</th>*@
                        <th style="color: white !important;">Polling Station</th>
                        <th style="color: white !important;">Panna Pramukh</th>
                        <th style="color: white !important;">Panna No</th>
                        <th style="color: white !important;">Village</th>
                        <th style="color: white !important;">Contact</th>
                        <th style="color: white !important;">Voter Id</th>
                        @*<th>Aadhar Number</th>*@
                        <th style="color: white !important;">Caste</th>
                        <th style="color: white !important;">Address</th>
                        <th style="color: white !important;">Image</th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        int sn = 1;
                        if (panna != null && panna.Any())
                        {
                            foreach (var item in panna)
                            {
                                <tr class="text-center align-middle">
                                    <td style="text-align:center!important;">@sn</td>
                                    <td>@item.MandalName</td>
                                    <td>@item.SectorName</td>
                                    <td style="text-align:center!important;">@item.BoothNumber</td>
                                    @*<td>@item.PollingStationBoothName</td>*@
                                    <td>@item.BoothName</td>
                                    <td>@item.Pannapramukh</td>
                                    <td>@item.PannaNumber</td>
                                    <td>@item.village</td>
                                    <td>@item.Mobile</td>
                                    <td>@item.VoterNumber</td>
                                    @*<td>@item.AadharNumber</td>*@
                                    <td>@item.SubCasteName</td>
                                    <td>@item.Address</td>
                                    <td>
                                        @if (!string.IsNullOrEmpty(item.ProfileImageUrl))
                                        {
                                            <img src="@item.ProfileImageUrl" class="img-thumbnail" style="max-width: 40px;" />
                                        }
                                        else
                                        {
                                            <span>No Image</span>
                                        }
                                    </td>
                                </tr>
                                sn++;
                            }
                        }
                        else
                        {
                            @*<tr>
                                    <td colspan="11" class="text-center">No panna pramukh records found.</td>
                                </tr>*@
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- JS Libraries -->
<script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
@*<script src="https://cdn.datatables.net/1.13.5/js/jquery.dataTables.min.js"></script>*@
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<!-- DataTables Buttons JS -->
@*<script src="https://cdn.datatables.net/buttons/2.4.1/js/dataTables.buttons.min.js"></script>
    <script src="https://cdn.datatables.net/buttons/2.4.1/js/buttons.flash.min.js"></script>*@
<script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/pdfmake.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/vfs_fonts.js"></script>
@*<script src="https://cdn.datatables.net/buttons/2.4.1/js/buttons.html5.min.js"></script>
    <script src="https://cdn.datatables.net/buttons/2.4.1/js/buttons.print.min.js"></script>*@
<script>
    $(document).ready(function () {

        let dt;
        let currentPage = 1;
        let limit = 100;

        initDataTable(limit);

        function initDataTable(limit) {

            dt = new DataTable('#pannaTable', {
                paging: true,
                pageLength: limit,
                lengthMenu: [100, 200, 300, 400, 500],
                searching: true,
                scrollX: true,
                scrollY: 300
            });

            dt.on('page.dt', function () {

                limit = getLimit();
                currentPage = dt.page() + 1;

                console.log("Page:", currentPage, "Limit:", limit);

                loadBackend(currentPage, limit);
            });
        }

        function getLimit() {
            return parseInt($(".dt-length select").val()) || 100;
        }

        $(document).on("change", ".dt-length select", function () {
            limit = getLimit();
            console.log("Limit changed:", limit);
        });

        function loadBackend(page, limit) {
            $.ajax({
                url: `/Sector/PannaListBySector?page=${page}&limit=${limit}`,
                type: "GET",
                success: function (html) {
                    $("#tableContainer").html(html);
                    reinitialize(limit);
                }
            });
        }

        function reinitialize(limit) {
            if ($.fn.DataTable.isDataTable('#pannaTable')) {
                $('#pannaTable').DataTable().destroy();
            }
            initDataTable(limit);
        }
    });
</script>


<!--Filter Script-->
<script>
    $(document).ready(function () {
        const urlParams = new URLSearchParams(window.location.search);

        // Split comma-separated values into arrays for multiple select
        //const boothNo = urlParams.get('boothIds') ? urlParams.get('boothIds').split(',') : [];
        const casteId = urlParams.get('casteIds') ? urlParams.get('casteIds').split(',') : [];

        // Set values
        //$('#boothFilter').val(boothNo).trigger('change');
        $('#casteFilter').val(casteId).trigger('change');

        //$('#boothFilter').select2({
        //    placeholder: "Select Booth No."
        //});

        $('#casteFilter').select2({
            placeholder: "Select Caste"
        });

        $('#filterButton').on('click', function () {
            debugger
            //var selectedBooth = $('#boothFilter').val();
            var selectedCaste = $('#casteFilter').val();
            // Check if both are empty
            if (
                (!selectedCaste || selectedCaste.length === 0)) {

                Swal.fire({
                    icon: 'warning',
                    title: 'No filter selected',
                    text: 'Please select at least one caste to filter data.',
                    confirmButtonText: 'OK'
                });

                return; // Stop further execution
            } else {

                const url = `${window.location.pathname}?casteIds=${selectedCaste}`;
                window.location.href = url;
            }
        });

        $('#resetFilters').click(function () {
            //$('#boothFilter').val('');
            $('#casteFilter').val('')

            // Reload page with empty query string
            window.location.href = window.location.pathname;
        });
    })
</script>

<script>
    $(document).ready(function () {

        // Excel Export
        $('#BoothDownloadExcel').on('click', function (e) {
            e.preventDefault();

            var selectedCaste = $('#casteFilter').val() ? $('#casteFilter').val().join(',') : "";

            var url = `/Export/sectorPannaPramukhExportReport?format=excel`
                + `&casteIds=${selectedCaste}`;

            console.log("Excel URL:", url);
            window.location.href = url;
        });

        // PDF Export
        $('#BoothDownloadPdf').on('click', function (e) {
            e.preventDefault();

            var selectedCaste = $('#casteFilter').val() ? $('#casteFilter').val().join(',') : "";

            var url = `/Export/sectorPannaPramukhExportReport?format=pdf`
                + `&casteIds=${selectedCaste}`;

            console.log("PDF URL:", url);
            window.location.href = url;
        });

        // Reset Filters
        $('#resetFilters').on('click', function () {
            $('#casteFilter').val([]).trigger("change");
            console.log("Filters Reset");
        });

    });
</script>
